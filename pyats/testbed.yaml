testbed:
  name: local-srl-lab

devices:
  srl1:
    alias: srl1
    os: linux
    type: generic
    connections:
      cli:
        protocol: ssh
        ip: YOUR_NEW_LIVE_PINGGY_SUBDOMAIN.run.pinggy-free.link
        port: YOUR_NEW_5_DIGIT_PORT
    credentials:
      default:
        username: admin
        password: admin

# Force update to trigger CI
